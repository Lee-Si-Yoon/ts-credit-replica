export interface Size {
  width: number;
  height: number;
}

export interface Coord {
  x: number;
  y: number;
}

export interface Sides {
  top: number;
  left: number;
  right: number;
  bottom: number;
}
